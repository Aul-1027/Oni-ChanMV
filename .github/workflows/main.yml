name: Release

on:
  workflow_dispatch:    # 手動実行を可能にする
  push:
    tags:
      - 'v*' 

jobs:
  release:
    runs-on: windows-latest
    permissions: write-all    # 権限を追加
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    
    - name: Build with PyInstaller
      run: |
        pyinstaller --name Oni-ChanMV --onefile --noconsole main.py
    
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.PAT }}    # PATを使用
      with:
        tag_name: ${{ github.ref_name }}    # タグ名の参照方法を修正
        release_name: Release ${{ github.ref_name }}
        draft: false
        prerelease: false
    
    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.PAT }}    # PATを使用
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./dist/Oni-ChanMV.exe
        asset_name: Oni-ChanMV.exe
        asset_content_type: application/octet-stream
